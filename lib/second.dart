import 'package:flutter/material.dart';

class MyWidgetKotak extends StatelessWidget {
  const MyWidgetKotak({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}